name: ubuntu
on: 
  workflow_dispatch:
defaults:
  run:
    shell: bash

jobs:
  build:
    timeout-minutes: 360
    runs-on: ubuntu-22.04
    steps:
    - name: Setup tmate session
      if: always()
      uses: mxschmitt/action-tmate@v2
    - name: Setup tmate session
      if: always()
      uses: mxschmitt/action-tmate@v2
    - name: Setup tmate session
      if: always()
      uses: mxschmitt/action-tmate@v2
    - name: Setup tmate session
      if: always()
      uses: mxschmitt/action-tmate@v2
    - name: Setup tmate session
      if: always()
      uses: mxschmitt/action-tmate@v2
